# Database Connection
# Example: postgresql://user:password@localhost:5432/sim_management
DATABASE_URL="postgresql://user:password@localhost:5432/sim_management"

# Public API Key (for external service integration - ICCID→MSISDN lookup)
# Generate with: openssl rand -hex 32
PUBLIC_API_KEY="your-secure-random-api-key-here"

# Encryption Key (for encrypting ServiceSource serviceRoleKey)
# MUST be at least 32 characters
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY="your-32-character-encryption-key-here"

# Application URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Environment
NODE_ENV="development"
